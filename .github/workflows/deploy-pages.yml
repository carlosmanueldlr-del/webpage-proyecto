name: Deploy to GitHub Pages (Sidebar nav preview)

on:
  push:
    branches:
      - claude/joana-sidebar-nav
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages-sidebar-nav
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          GITHUB_PAGES: "true"
      - name: Deploy to gh-pages branch (sidebar-nav/ subfolder)
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out
          destination_dir: sidebar-nav
          keep_files: true
